schema test5_schema {
	# no delimiter -- I'm trying to see if we can have ends-of-records
	# defined purely by the field count

	delimiter = [];
	separator = [	]+;

	attr rider {
		type = string;
		maxlen = 25;
	}

	attr race {
		type = string;
		maxlen = 25;
	}
}
