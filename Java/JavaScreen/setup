#! /bin/csh -f

#  ========================================================================
#  DEVise Data Visualization Software
#  (c) Copyright 2000
#  By the DEVise Development Group
#  Madison, Wisconsin
#  All Rights Reserved.
#  ========================================================================
#
#  Under no circumstances is this software to be copied, distributed,
#  or altered in any way without prior permission from the DEVise
#  Development Group.

#  This script sets up this directory to run the JavaScreen (by making
#  appropriate links).

############################################################

#  $Id$

############################################################

#TEMP -- move this to a Makefile
# have a Makefile.config

if (! -e bin) then
  mkdir bin
endif 

if (-e bin/devised) then
  rm bin/devised
endif

cd bin
#TEMP -- get rid of hard-coded linux_rhel5
ln -s ../../../linux_rhel5/generic/devised .
if (! -e Tasvir) then
  ln -s /p/devise/public/bin/Tasvir .
endif
cd ..

if (! -e lib) then
  mkdir lib
endif 

if (! -e lib/composite.ini) then
  cd lib
  ln -s ../../../lib/composite.ini .
  cd ..
endif

if (! -e JavaScreen) then
  mkdir JavaScreen
endif
cd JavaScreen
ln -s ../*.gif .
ln -s ../resources .
if (! -e UI) then
  mkdir UI
endif
cd UI
ln -s ../../UI/*.java .
cd ../..
