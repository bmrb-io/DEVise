#! /bin/csh -f

#  ========================================================================
#  DEVise Data Visualization Software
#  (c) Copyright 2000
#  By the DEVise Development Group
#  Madison, Wisconsin
#  All Rights Reserved.
#  ========================================================================
#
#  Under no circumstances is this software to be copied, distributed,
#  or altered in any way without prior permission from the DEVise
#  Development Group.

#  This script sets up this directory to run the JavaScreen (by making
#  appropriate links).

############################################################

#  $Id$

############################################################

# Note: -e test doesn't work on symlink.
if (`ls devised` == devised) then
  rm devised
endif

ln -s ../../$ARCH/generic/devised .

if (! -e lib) then
  mkdir lib
endif 

if (! -e lib/composite.ini) then
  cd lib
  ln -s ../../../lib/composite.ini .
  cd ..
endif
