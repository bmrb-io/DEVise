			Multi-Entry Visualizations
				2010-11-05

The first step is just to get things working with the possiblity of
specifying more than one BMRB ID.

What are "ID units" of processing?:
- BMRB ID
- BMRB ID and PDB ID (coordinates, restraints, chem shift ref)
- Filename and name (visualization server)
- Filename and name plus PDB ID (visualization server w/ coordinates,
  restraints, or chem shift ref)
- Other (PDB only?

What do we need to add?:
- Two (or maybe more) BMRB IDs.
- Filename and name plus BMRB ID (visualization server)
