#! /bin/csh -f

# Test an entry with RNA only.

set result = 0

rm -r -f */15080*

echo -n "Processing NMR-STAR and mmCIF: "
./s2d -nomail -name 15080 -file test_data/bmr15080_3.str -force -check_pct \
  -do_pdb 2
if ($status != 0) then
  echo "First invocation of s2d failed!"
  set result = 1
endif

source tests/test67_check


echo -n "Test67_3 result: "
if ($result == 0) then
  echo "OK"
  exit 0
else
  echo "FAILED"
  exit 1
endif
