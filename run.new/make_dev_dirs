#! /bin/csh -f
############################################################

# Make the DEVise temp and work directories if they don't
# already exist.

# $Id$

############################################################

if (! -e $DEVISE_TMP) then
  mkdir $DEVISE_TMP
endif

if (! -e $DEVISE_WORK) then
  mkdir $DEVISE_WORK
endif
