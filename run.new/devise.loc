#! /bin/csh -f
############################################################

# Run the local DEVise with the local data, sessions, etc.

# $Id$

############################################################

limit coredumpsize 0
limit descriptors 256

source dev_prog.loc
source dev_vis.loc

source start_mono
