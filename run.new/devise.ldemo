#! /bin/csh -f
############################################################

# Run the local DEVise with the demo data, sessions, etc.

# $Id$

############################################################

# limit coredumpsize 0
limit descriptors 256

set devise_run = `dirname $0`

source $devise_run/dev_prog.loc
source $devise_run/dev_vis.demo

source $devise_run/start_mono
