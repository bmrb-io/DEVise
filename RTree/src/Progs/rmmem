#!/s/perl/bin/perl -w

$rm = 'ipcrm';
foreach (grep(/$ENV{'USER'}/, `ipcs`)) {
    ($type, $id) = split;
    $rm .= " -$type $id";
}
if( $rm ne 'ipcrm' ) {
    print "$rm\n";
    system($rm);
} else {
    print "no resources to remove\n";
}
